import { PasswordStyleDirective } from './password-style.directive';

describe('PasswordStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordStyleDirective();
    expect(directive).toBeTruthy();
  });
});
